services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: orders_frontend
    ports:
      - "4200:80"
    networks:
      - orders_network

networks:
  orders_network:
    external: true
